{
    "name": "caprover-e2e",
    "version": "0.1.0",
    "private": true,
    "description": "External end-to-end tests for a provisioned CapRover server",
    "license": "MIT",
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "test": "npm run typecheck && npm run test:all",
        "test:unit": "vitest run --mode unit",
        "test:ephemeral": "npm run build:provisioning && node .provisioning-dist/provisioning/commands/test-ephemeral.js",
        "provision": "npm run build:provisioning && node .provisioning-dist/provisioning/commands/provision.js",
        "diagnose": "npm run build:provisioning && node .provisioning-dist/provisioning/commands/diagnose.js",
        "destroy": "npm run build:provisioning && node .provisioning-dist/provisioning/commands/destroy.js",
        "build:provisioning": "tsc -p tsconfig.provisioning.json",
        "typecheck": "tsc --noEmit",
        "format": "prettier --check .",
        "format:write": "prettier --write .",
        "test:smoke": "vitest run --mode smoke",
        "test:core": "vitest run --mode core",
        "test:nginx-reload-race": "vitest run --mode nginx-reload-race",
        "test:destructive": "vitest run --mode destructive",
        "test:git-webhooks": "vitest run --mode git-webhooks",
        "test:all": "vitest run --mode all"
    },
    "dependencies": {
        "caprover-api": "0.0.25",
        "ssh2": "1.17.0"
    },
    "devDependencies": {
        "@types/node": "22.20.3",
        "@types/ssh2": "1.15.6",
        "prettier": "3.9.6",
        "typescript": "5.9.3",
        "vitest": "5.0.1"
    }
}
